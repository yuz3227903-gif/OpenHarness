# OpenHarness 多 Agent 投研项目：新电脑迁移与启动说明

本文用于让同事或另一台 Windows 电脑从 GitHub 分支拉取代码，完成首次配置，并运行七 Agent 的 CrewAI + OpenHarness 完整投研链路。

## 1. 仓库与分支

- 仓库：`https://github.com/yuz3227903-gif/OpenHarness.git`
- 开发分支：`feature/bojiboji`
- Python：建议 3.11 或 3.12，项目最低要求为 3.10
- 首版不需要 Docker

```powershell
git clone https://github.com/yuz3227903-gif/OpenHarness.git
cd OpenHarness
git switch feature/bojiboji
git branch --show-current
```

最后一条命令应显示：

```text
feature/bojiboji
```

## 2. 当前代码包含什么

该分支已包含：

- 七个投研 Agent：Planner、Fundamental、IndustryCompetition、MarketCatalyst、Risk、ReviewerArbiter、ReportWriter；
- 七个 Agent 的角色提示词、输入输出合同和工具权限；
- OpenHarness Agent 执行层；
- CrewAI 完整流程编排；
- 三个研究 Agent 并行执行；
- Risk 串行消费上游成果；
- Reviewer 审查、定向补充和最终复审；
- DeepSeek OpenAI-compatible 模型调用；
- Tavily 搜索、网页读取、文件读取、计算和证据查询工具；
- SQLite 证据存储；
- 网络错误、空回复、JSON/Schema 错误的有限重试和降级；
- ReportWriter 正式报告生成；
- ReportWriter 失败时的本地兜底报告；
- 按 Run 保存报告、摘要和紧凑报告材料包；
- 自动测试。

完整流程为：

```text
Planner
  → Fundamental / IndustryCompetition / MarketCatalyst（并行）
  → Risk
  → ReviewerArbiter
  → 必要时定向补充一次
  → ReviewerArbiter 最终复审
  → ReportWriter
  → 失败时本地兜底报告
```

## 3. 安装 Python 环境与依赖

在项目根目录运行：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -e ".[orchestration,dev]"
.\.venv\Scripts\python.exe -m pip check
```

`orchestration` 会安装项目锁定的 CrewAI 版本，当前为 `crewai==1.15.5`；不需要再单独手工安装 CrewAI。

如果公司网络导致安装失败，应先检查代理或 PyPI 访问，不要删除源码或重建框架。

## 4. 配置 DeepSeek

API Key 不在 GitHub 中。新电脑必须自行配置，不能复制旧电脑的本地凭据文件，也不能把 Key 写进代码、Markdown 或提交记录。

推荐使用 OpenHarness 自带的交互配置：

```powershell
.\.venv\Scripts\oh.exe provider add deepseek `
  --label "DeepSeek" `
  --provider openai `
  --api-format openai `
  --auth-source openai_api_key `
  --model deepseek-v4-flash `
  --base-url https://api.deepseek.com

.\.venv\Scripts\oh.exe setup deepseek
.\.venv\Scripts\oh.exe provider use deepseek
.\.venv\Scripts\oh.exe provider list
```

在交互步骤中输入自己的 DeepSeek API Key。不要把 Key 直接写进 PowerShell 命令，以免进入命令历史。

配置后应确认活动 Provider 为 `deepseek`，模型为 `deepseek-v4-flash`。如果账号实际不支持该模型，应改成账号可调用的兼容模型，并同步检查验证标准。

## 5. 配置 Tavily

项目提供了 Windows DPAPI 加密脚本：

```powershell
.\setup-tavily-key.ps1
```

按提示输入自己的 Tavily API Key。脚本会把密钥加密保存到：

```text
.openharness/secrets/tavily-key.dpapi
```

该密文只对同一台电脑上的同一 Windows 用户有效，因此不能从旧电脑复制，也不能上传 GitHub。

## 6. 先运行自动测试

```powershell
.\.venv\Scripts\python.exe -m pytest tests\invest_research tests\test_api\test_openai_client.py -q
```

提交版本在旧电脑上的发布前结果为：

```text
106 passed, 152 subtests passed
```

pytest 缓存目录的权限警告不影响功能；真正的测试失败不能忽略。

## 7. 运行完整七 Agent 链路

```powershell
.\run-investment-research.ps1 -CrewAIFullChainValidation `
  -Company "宁德时代" `
  -AsOfDate "2026-08-11"
```

这是联网长任务，运行期间可能数分钟没有新输出。不要反复启动多个相同任务，也不要在模型仍运行时关闭窗口。

运行结束后查看兼容路径：

```powershell
Get-Content ".openharness\validation\full-chain.json" -Raw
notepad ".openharness\validation\full-chain-report.md"
```

每次运行还会保存独立目录：

```text
.openharness/validation/{run_id}/report.md
.openharness/validation/{run_id}/summary.json
.openharness/validation/{run_id}/report-context.json
```

重点检查：

```text
report_generated = true
pass = true
fallback_used = false   # 表示由真实 ReportWriter 生成
```

如果 `fallback_used = true`，说明系统仍交付了兜底报告，但 ReportWriter 本次没有正常完成；应查看 `failed_agents`、`failure_class` 和 `warnings`。

## 8. 只补跑 ReportWriter

如果前六个 Agent 已成功，仅 ReportWriter 因网络波动、超时或空回复失败，不需要重新跑全部研究。

从 `full-chain.json` 找到 Run ID，然后运行：

```powershell
.\run-investment-research.ps1 -ReportWriterSmoke `
  -RunId "RUN-CREWAI-XXXXXXXXXXXX"
```

它会复用该 Run 的证据，执行：

```text
紧凑报告材料包 → DeepSeek ReportWriter → Schema 校验 → Markdown
```

ReportWriter 没有搜索权限，工具调用数量应为 0。

## 9. 其他验证命令

Planner：

```powershell
.\run-investment-research.ps1 -PlannerSmoke `
  -Company "宁德时代" `
  -AsOfDate "2026-08-11"
```

单个 Agent：

```powershell
.\run-investment-research.ps1 -AgentSmoke fundamental
.\run-investment-research.ps1 -AgentSmoke industry_competition
.\run-investment-research.ps1 -AgentSmoke market_catalyst
.\run-investment-research.ps1 -AgentSmoke risk
.\run-investment-research.ps1 -AgentSmoke reviewer_arbiter
.\run-investment-research.ps1 -AgentSmoke report_writer
```

全部 Agent：

```powershell
.\run-investment-research.ps1 -ValidateAllAgents
```

最小 CrewAI 编排：

```powershell
.\run-investment-research.ps1 -CrewAIFlowSmoke `
  -Company "宁德时代" `
  -AsOfDate "2026-08-11"
```

## 10. 关于 `run-openh.ps1`

该脚本会寻找 Codex 桌面环境内置的 Node.js 路径，因此换电脑后可能无法直接用于 OpenHarness 的终端界面。

七 Agent 投研完整链路不依赖这个 Node 路径，直接运行 `run-investment-research.ps1` 即可。若后续需要 OpenHarness 交互终端，应在新电脑单独安装 Node.js，并调整 `run-openh.ps1` 的 Node 路径或直接运行虚拟环境中的 `oh.exe`。

## 11. Git 不会带走的本地内容

以下内容被有意排除，拉取分支后不会存在：

```text
.venv/
.env
.openharness/secrets/
.openharness/data/
.openharness/validation/
API Key
SQLite 数据库
运行日志
模型回执
生成的研究报告
缓存
```

这不是代码缺失，而是安全和可迁移性要求。新电脑要重新创建 `.venv`、配置两个 API Key，并重新运行后生成自己的本地数据和报告。

## 12. 开发注意事项

- CrewAI 只负责业务编排；OpenHarness 负责单 Agent 执行、工具和 Schema 校验。
- 不启用 OpenHarness 自由子 Agent 调度，避免出现两个调度者。
- 不把聊天记录当正式证据；正式链路为 `Source → Fact → Logic/Risk → Review → Report`。
- Agent 失败可以降级继续，但不能伪装为成功。
- Reviewer 不阻断报告交付，未解决问题进入警告和待验证事项。
- 任何自动重试都有次数上限，避免无限等待。
- 不要提交密钥、本地数据库、验证回执和生成报告。
- 修改前先运行 `git status`，保留他人已有改动。

## 13. 最小验收顺序

在新电脑上建议按以下顺序验收：

```text
1. 切换 feature/bojiboji
2. 创建 .venv 并安装 [orchestration,dev]
3. 配置 DeepSeek
4. 配置 Tavily
5. 自动测试通过
6. PlannerSmoke 成功
7. CrewAIFullChainValidation 生成报告
8. 检查 full-chain.json 和报告正文
```

做到第 7 步后，才可以说“这台电脑已经完整运行七 Agent 投研流程”。
